<h1>Create All Shifts</h1>


<%= form_for(@shift_time) do |f| %>
      <%= render 'shared/error_messages', the_model: @shift_time %>

      <table>

      	<tr>
      		<th>Start Time</th>
			<th>End Time</th>
      	</tr>

<!-- # in projects/new.rhtml
<% @project.tasks.each_with_index do |task, index| %>
  <% fields_for "tasks[#{index}]", task do |f| %>
    <p><%= f.text_field :name %></p>
  <% end %>
<% end %> -->


      	<% @shift_times.count.times do |i| %>
			<tr>
				<td>
					<%= f.text_field :start, placeholder: "Start", value: @shift_times[i].start, size: 10 %>
				</td>
				<td>
					<%= f.text_field :end, placeholder: "End",
					value: @shift_times[i].end, size: 10 %>	
				</td>
			</tr>
		<% end %>

			<tr>
				<td>
					<%= f.text_field :start, placeholder: "Start", size: 10 %>
				</td>
				<td>
					<%= f.text_field :end, placeholder: "End",
					 size: 10 %>	
				</td>
			</tr>

		<tr>  <%= submit_tag("Create All Shifts!") %> </tr>
	</table>

<% end %>
